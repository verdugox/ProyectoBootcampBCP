spring:
  config:
    activate:
      on-profile: prod

  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://mongo:27017/bankx}

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
