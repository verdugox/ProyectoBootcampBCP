apiVersion: v1
kind: Service
metadata:
  name: transactions-service
  labels:
    app: transactions-service
spec:
  type: ClusterIP
  selector:
    app: transactions-service
  ports:
    - name: http
      port: 80          # 👈 PUERTO DEL SERVICE
      targetPort: 8085  # 👈 PUERTO REAL DEL POD
